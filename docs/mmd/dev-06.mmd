flowchart TB
  subgraph READ[读侧]
    R1[元数据携带 dev_idx] --> R2[解析设备]
    R2 --> R3[获取 READ 引用]
    R3 --> R4[优先在线 / 健康 / 低延迟副本]
    R4 --> R5[失败时重试其他副本]
  end

  subgraph WRITE[写侧]
    W1[分配器选择 rw_devs] --> W2[按目标掩码过滤]
    W2 --> W3[副本 指针 列表]
    W3 --> W4[为每个 指针 获取引用]
    W4 --> W5[提交克隆的 bio]
    W5 --> W6[所有副本完成后结束]
  end

  subgraph RECOVERY[恢复侧]
    C1[超级块 + 日志 + 分配器] --> C2[阶段调度器]
    C2 --> C3[节点扫描 + 检查]
    C3 --> C4[修复缺失或过期副本]
  end
